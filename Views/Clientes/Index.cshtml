@model IEnumerable<Inventario.Models.Cliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Clientes</h1>

<p>
    <a asp-action="Create" class="btn btn-primary"><i class="fa fa-plus"></i></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                Correo Electronico
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefono)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CorreoElectronico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.ClienteId"><i class="fas fa-pen"></i></a> |
                    <a asp-action="Details" asp-route-id="@item.ClienteId"><i class="fas fa-eye"></i></a> |
                    <a asp-action="Delete" asp-route-id="@item.ClienteId"><i class="fas fa-trash"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
