@model IEnumerable<Inventario.Models.Pedido>

@{
    ViewData["Title"] = "Index";
}

<h1>Pedidos</h1>

<p>
    <a asp-action="Create" class="btn btn-primary"><i class="fa fa-plus"></i></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaPedido)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaPedido)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PedidoId"><i class="fas fa-pen"></i></a> |
                <a asp-action="Details" asp-route-id="@item.PedidoId"><i class="fas fa-eye"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.PedidoId"><i class="fas fa-trash"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
